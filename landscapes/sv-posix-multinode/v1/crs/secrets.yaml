# ============================================================
# DEMO CREDENTIALS -- PLAINTEXT, NOT FOR PRODUCTION.
# ============================================================
# it.176 (maintainer's call, for scaffolding velocity): every credential
# below is a fixed, committed, non-secret demo value. NEVER put a real
# credential in this file or this repo. A real fork of this model would
# replace this file with sealed-secrets/SOPS/External-Secrets-Operator
# output instead of a plain Secret manifest -- see the top-level
# README's opening warning.
apiVersion: v1
kind: Secret
metadata:
  name: onezone-admin-account
  namespace: sv-posix-multinode
  annotations:
    argocd.argoproj.io/sync-wave: "2"
type: Opaque
stringData:
  username: admin
  password: demo-onezone-admin-password
---
apiVersion: v1
kind: Secret
metadata:
  name: onezone-onepanel-creds
  namespace: sv-posix-multinode
  annotations:
    argocd.argoproj.io/sync-wave: "2"
type: Opaque
stringData:
  username: onepanel
  # Doubles as the emergency passphrase bootstrapped on the managed
  # Onezone's first boot -- see ManagedDeployment's doc comment in the
  # operator repo's api/v1alpha1/oneprovider_types.go.
  password: demo-onezone-onepanel-password
---
apiVersion: v1
kind: Secret
metadata:
  name: provider-onepanel-creds
  namespace: sv-posix-multinode
  annotations:
    argocd.argoproj.io/sync-wave: "2"
type: Opaque
stringData:
  username: onepanel
  password: demo-provider-onepanel-password
---
apiVersion: v1
kind: Secret
metadata:
  name: scientist-attrs
  namespace: sv-posix-multinode
  annotations:
    argocd.argoproj.io/sync-wave: "2"
type: Opaque
stringData:
  password: demo-scientist-password
  fullName: "Demo Scientist"
