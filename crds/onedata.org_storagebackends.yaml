---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: storagebackends.onedata.org
spec:
  group: onedata.org
  names:
    kind: StorageBackend
    listKind: StorageBackendList
    plural: storagebackends
    singular: storagebackend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.providerRef.name
      name: Provider
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.storageId
      name: StorageID
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageBackend is the Schema for the storagebackends API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StorageBackend
            properties:
              credentialsSecretRef:
                description: |-
                  credentialsSecretRef references the Secret holding this storage's
                  access credentials (e.g. accessKey/secretKey for s3, username/key
                  for ceph), kept out of params so they are not stored in plaintext.
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              importedStorage:
                default: false
                description: |-
                  importedStorage, when true, declares that the storage already
                  contains data to be imported into a space rather than being
                  managed exclusively by Oneprovider.
                type: boolean
              luma:
                description: |-
                  luma configures the Local User Mapping database feed for this
                  storage.
                properties:
                  feed:
                    default: auto
                    description: feed selects the source of user/group mappings for
                      this storage.
                    enum:
                    - auto
                    - local
                    - external
                    type: string
                  lumaRef:
                    description: |-
                      lumaRef references a Secret (in the same namespace) holding the
                      external LUMA feed URL and API key. Relevant only when feed is
                      "external".
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              name:
                description: |-
                  name is the storage's name as registered in Onepanel. If empty, the
                  CR's own metadata.name is used. An explicit name is needed when the
                  desired Onepanel storage name is not a valid Kubernetes object name
                  -- e.g. one-env's "IdealNullStorage" (uppercase), which Onedata's
                  CT suites reference verbatim -- or when several providers in one
                  namespace each need a storage of the same name (e.g. "posix"),
                  which distinct CR names plus an identical spec.name make possible.
                type: string
              onepanelCredentialsSecretRef:
                description: |-
                  onepanelCredentialsSecretRef references the Secret (in the same
                  namespace unless the reference sets its own namespace) holding
                  basic-auth credentials -- keys "username" and "password" -- used to
                  authenticate against onepanelEndpoint.

                  TEMPORARY / MVP / drive-first field: see onepanelEndpoint's doc
                  comment.
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              onepanelEndpoint:
                description: |-
                  onepanelEndpoint is the HTTPS URL (or bare host[:port]) of the
                  existing, already-deployed onepanel instance for the Oneprovider
                  identified by providerRef, e.g. "https://oneprovider.example.com:9443".

                  TEMPORARY / MVP / drive-first field: mirrors
                  Oneprovider.spec.onepanelEndpoint (see its doc comment in
                  oneprovider_types.go) rather than being derived from providerRef, so
                  this controller can reconcile independently before the operator
                  gains the ability to resolve a Oneprovider's onepanel endpoint from
                  the referenced resource's own status. Expected to become unnecessary
                  once that resolution exists.
                type: string
              params:
                additionalProperties:
                  type: string
                description: |-
                  params carries the type-specific storage helper configuration as
                  string key/value pairs (e.g. mountPoint for posix; hostname,
                  bucketName for s3). Keys correspond to the fields of the matching
                  Onepanel swagger storage model. Non-sensitive settings only;
                  secrets belong in credentialsSecretRef.
                type: object
              providerRef:
                description: |-
                  providerRef identifies the Oneprovider custom resource (in the same
                  namespace) that will host this storage backend.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              readonly:
                default: false
                description: |-
                  readonly, when true, blocks all write/modify/delete operations on
                  the storage. Such a storage can only be used to import data.
                  Available only for imported storages.
                type: boolean
              type:
                description: |-
                  type is the storage helper type. It determines which keys are
                  expected in params.
                enum:
                - posix
                - nfs
                - s3
                - ceph
                - cephrados
                - swift
                - glusterfs
                - webdav
                - http
                - xrootd
                - nulldevice
                type: string
            required:
            - providerRef
            - type
            type: object
          status:
            description: status defines the observed state of StorageBackend
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the StorageBackend
                  resource. Each condition has a unique type and reflects the status
                  of a specific aspect of the resource.

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: |-
                  phase summarizes the current lifecycle state of the storage
                  backend registration.
                enum:
                - Pending
                - Deploying
                - Ready
                - Degraded
                type: string
              storageId:
                description: |-
                  storageId is the Oneprovider-assigned identifier for this storage,
                  populated once it is registered via Onepanel.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
